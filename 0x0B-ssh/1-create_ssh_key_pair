#!/bin/bash

# Generate RSA key pair with specified parameters
openssl genrsa -aes256 -passout pass:betty -out school.pem 4096

# Extract public key from private key
openssl rsa -in school.pem -pubout -out school.pub.pem

